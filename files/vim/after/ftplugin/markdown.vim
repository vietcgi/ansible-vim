" Markdown (MD) script config
setlocal spell spelllang=en_ca

setlocal tabstop=4
setlocal shiftwidth=4

setlocal expandtab  " Convert tabs to spaces

setlocal wrap       " Show long lines as multiple lines
